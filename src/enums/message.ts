export enum MESSAGE_TYPES {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  SYSTEM = 'SYSTEM',
}

export enum MESSAGE_STATUS {
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

export enum MESSAGE_FLOW {
  IN = 'IN',
  OUT = 'OUT',
}
